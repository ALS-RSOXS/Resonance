# API Reference

The API reference is split into two sections:

- **Resonance API**: modules under `src/resonance/api`
- **MCP**: modules under `src/resonance/mcp`

Each file has its own page in the navigation tree. `__init__.py` files are intentionally excluded.
